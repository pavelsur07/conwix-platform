# PROJECT RESET v1.4 — CONWIX

Проект: **Conwix**  
Тип: SaaS-платформа  
Статус: Active (MVP)  
Версия контекста: **v1.4** — baseline

---

## 🎯 Цель проекта

**Conwix** — SaaS-платформа для управления продажами и поддержкой через диалоги.

Conwix создаётся как **диалого-центричная система**, где:

- диалог — первичная сущность,
- из диалога формируются клиенты, события и действия,
- автоматизация и AI усиливают человека, а не заменяют его.

### Conwix предоставляет бизнесу единый инструмент, который объединяет:

- коммуникацию с клиентами (мессенджеры и каналы),
- CRM, выстроенную вокруг диалогов,
- сценарии общения (BotFlow),
- событийную модель и систему уведомлений,
- инструменты автоматизации продаж и поддержки.

### Ключевой принцип продукта

> **Диалог — это основной актив бизнеса и точка управления.**

Платформа помогает бизнесу:

- быстрее реагировать на клиентов,
- снижать ручную нагрузку,
- не терять диалоги и сделки,
- автоматизировать рутину **без потери контроля**,
- сохранять прозрачность принятия решений.

---

## 🧠 Контекст проекта (строгие правила)

Разрешено использовать **только**:

- текущие обсуждения и артефакты проекта Conwix,
- документацию в каталоге `docs/`,
- решения, зафиксированные через ADR (`docs/decisions/`).

Запрещено:

- использовать контекст и решения других проектов  
  (Fin-Plan, Ваш ФинДиректор, Womjoy и т.п.),
- “подтягивать” архитектуру, паттерны и правила без фиксации здесь,
- додумывать поведение системы без явных требований.

Если информации не хватает → **задавать вопрос**, а не предполагать.

---

## 🏗 Технологический контур (MVP)

### Backend
- Symfony
- Doctrine ORM
- PostgreSQL
- Redis

### Frontend
- Twig — основной UI
- React — точечно:
  - Chat Center
  - BotFlow  
    Расположение: `app/assets/react/*`

### Realtime
- Socket.IO (отдельный сервис)
- Redis Pub/Sub

### Инфраструктура
- Docker
- docker compose
- Traefik

---

## 📦 Репозиторий и инфраструктура

### Monorepo

Весь проект живёт в **одном репозитории**.

Структура репозитория и инфраструктуры зафиксирована в:

- **ADR-0001**  
  `docs/decisions/adr-0001-repo-structure.md`

### Dev-окружение (важно)

**Dev-режим намеренно упрощён.**

Правила:

- используется **один** файл:
  `infra/docker-compose.yml`
- команды **одинаковые всегда**:
  `docker compose up -d` / `make up`
- **никаких `.env` в dev**
- все переменные окружения **жёстко прописаны** в `docker-compose.yml`
- CLI-контейнер **не висит**, используется через:
  `docker compose run --rm app-php-cli ...`

Dev-окружение должно быть:
- предсказуемым,
- легко воспроизводимым,
- без скрытой магии.

---

## 🧱 Модульность (MVP, без усложнений)

### Цель

> **Код одной фичи не должен быть размазан по всему проекту.**

### Backend

- модуль создаётся **только при появлении реальной функциональности**,
- весь backend-код модуля размещается в:
  `app/src/Modules/<ModuleName>/`

Запрещены “общие свалки” в корне `app/src/`:

- `Service/`
- `Utils/`
- `Helpers/`
- `Manager/`

### Frontend (React)

- React-части считаются “модулями” **на уровне документации**,
- физически располагаются в:
  `app/assets/react/<feature>/`

### Документация

- ADR-0002: структура модулей  
  `docs/decisions/adr-0002-module-structure.md`
- ADR-0003: правила модулей  
  `docs/decisions/adr-0003-module-rules.md`

---

## 🧪 Тестирование (обязательно с MVP)

Тестирование — **часть архитектуры**, а не опция.

### Базовые принципы

- тесты пишутся вместе с кодом,
- используется подход **builders-first**,
- сущности создаются через Builders,
- моки применяются **только** на внешних границах,
- бизнес-логику тестируем через реальные сущности.

### Документация

- ADR-0004: стратегия тестирования  
  `docs/decisions/adr-0004-testing-strategy.md`
- Checklist code review:  
  `docs/development/code-review-tests.md`

---

## 🚫 Anti-goals (что мы сознательно НЕ делаем)

Этот раздел используется для **отсечения лишних решений и задач**.

1. Conwix — **не классическая CRM**  
   CRM вторична и вырастает из диалогов и событий.

2. Conwix — **не просто чат**  
   Чат — точка управления, а не UI ради UI.

3. Conwix — **не бот-конструктор ради ботов**  
   BotFlow всегда подчинён бизнес-сценарию и человеку.

4. Conwix — **не автономный AI**  
   Человек всегда остаётся в контуре управления.

5. Conwix — **не платформа “для всего сразу”**  
   Интеграции добавляются по приоритету и ценности.

6. Conwix — **не добавляет AI ради галочки**  
   AI используется только при измеримой пользе.

7. Conwix — **не overengineered-проект**  
   Усложнение — только при реальной необходимости.

8. Conwix — **не живёт без правил**  
   Все изменения фиксируются через ADR.

---

## 📐 Архитектурные принципы

- MVP-first
- 1 задача → 1 изменение → 1 смысл
- контроллеры не содержат бизнес-логики
- временные решения либо фиксируются, либо запрещены
- новые правила действуют **только после фиксации в docs**

---

## 🛑 Стоп-лист (нельзя)

- фантазировать о несуществующем коде,
- менять структуру и нейминг без ADR,
- создавать “общие” каталоги в `app/src/`,
- переносить решения из других проектов,
- добавлять код без тестов без объяснения,
- усложнять архитектуру без запроса и фиксации.

---

## ✅ Режим работы

- работать строго в рамках Conwix,
- давать прикладные и проверяемые решения,
- не додумывать за продукт,
- при сомнениях — задавать вопрос.

---

## 🔐 Подтверждение

С этого документа:

- контекст Conwix считается изолированным,
- внешние проекты игнорируются,
- любые изменения правил фиксируются через ADR
  в `docs/decisions/`.
